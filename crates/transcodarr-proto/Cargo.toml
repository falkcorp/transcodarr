# file: crates/transcodarr-proto/Cargo.toml
# version: 1.0.0
# guid: 5a71e0c4-836b-4d92-a017-4f2c95e18b60
# last-edited: 2026-08-03

[package]
name = "transcodarr-proto"
version = "0.1.0"
description = "transcodarr agent protocol: message types, version gate, conversions."
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true

# No tonic or prost yet, and deliberately so -- see the crate documentation.
# The wire schema lives in proto/, the semantics live here, and codegen is added
# by whoever wires the transport.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
transcodarr-core = { path = "../transcodarr-core" }
