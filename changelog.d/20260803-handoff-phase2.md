### Changed

#### Handoff records Phase 2 progress

Schema, migrations and `Writer` are done; `ReadPool`, repositories, `Scanner`
and `Evaluator` remain, listed in build order with the constraints that apply to
each. Also records the two deliberate deviations made so far and why.
